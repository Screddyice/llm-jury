__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.eggs/
.llmjury/
.DS_Store
*.log
.venv/
venv/
# Local agent scaffolding — one contributor's session state, not project config.
# Ignored whole rather than per-subdirectory: it is generated per machine, so any
# of it that lands in a commit is noise for everyone else.
.claude-harness/
CLAUDE.md

# Claude Code - Local settings
.claude/settings.local.json
.gstack/
